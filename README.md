# oci-cloud-guard
OCI Cloud Guard is a security posture management
In a scenario - security team plans to export all the OCI cloud guard dected problems to an xls and post it to object storage.
Leveraging this code - we can export all the problems dected by OCI CG and post the xls to object storage
